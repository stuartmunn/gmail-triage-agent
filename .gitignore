# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
.venv/
venv/
build/
dist/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# Env / secrets
.env
.env.*
*.env
credentials.json
token.json

# Personal triage data — the live known-senders list holds real contacts;
# only the .example template is tracked.
data/known-senders.md

# Live model pricing override — only the .example template is tracked; the
# host copies it to data/pricing.json and edits when models are repriced.
data/pricing.json

# Runtime data written by the agent — logs and the last-success marker.
data/logs/
data/state/

# Editors / OS
.vscode/
.idea/
.DS_Store
.pylibs/
