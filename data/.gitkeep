# This directory is bind-mounted into the container at runtime for anything
# the agent reads/writes live (e.g. known-senders list, logs). Kept in git as
# an empty mount point; its live contents are not tracked here.
